---
phase: 02-python-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - py/Cargo.toml
  - py/pyproject.toml
  - py/src/lib.rs
  - py/src/errors.rs
autonomous: true

must_haves:
  truths:
    - "PyO3 0.27 compiles without deprecation warnings for GIL Refs"
    - "pyo3-async-runtimes is available for future_into_py usage"
    - "Python binding builds successfully with maturin 1.11+"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace-level PyO3 0.27 and pyo3-async-runtimes dependencies"
      contains: 'pyo3 = { version = "0.27"'
    - path: "py/Cargo.toml"
      provides: "Python binding crate with async runtime support"
      contains: "pyo3-async-runtimes"
    - path: "py/src/lib.rs"
      provides: "Module entry point using Bound API"
      contains: "Bound<'_, PyModule>"
    - path: "py/src/errors.rs"
      provides: "Exception hierarchy foundation"
      contains: "create_exception!"
  key_links:
    - from: "py/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace dependency inheritance"
      pattern: "pyo3\\.workspace = true"
---

<objective>
Upgrade Python binding foundation to PyO3 0.27 with async runtime support

Purpose: Establish the modern PyO3 foundation required for native asyncio support. PyO3 0.27 introduces Bound smart pointers (replacing deprecated GIL Refs) and pyo3-async-runtimes enables tokio-to-asyncio bridging via future_into_py().

Output: Compiling Python binding with PyO3 0.27, pyo3-async-runtimes 0.27, and maturin 1.11+
</objective>

<execution_context>
@/Users/zackfan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackfan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-python-binding/02-RESEARCH.md
@Cargo.toml
@py/Cargo.toml
@py/pyproject.toml
@py/src/lib.rs
@py/src/errors.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade workspace dependencies to PyO3 0.27</name>
  <files>Cargo.toml, py/Cargo.toml, py/pyproject.toml</files>
  <action>
Update workspace-level dependencies in root Cargo.toml:
- Upgrade pyo3 from 0.22 to 0.27 with extension-module feature
- Add pyo3-async-runtimes 0.27 with tokio-runtime feature
- Ensure tokio features include rt-multi-thread, macros, time (already present)

In py/Cargo.toml:
- Add pyo3-async-runtimes = { workspace = true, features = ["tokio-runtime"] }
- Keep existing workspace inheritance for pyo3, tokio, serde_json, marketdata-core

In py/pyproject.toml:
- Update maturin requirement from ">=1.0,<2.0" to ">=1.11,<2.0"
- Add optional-dependencies for dev: pytest, pytest-asyncio

IMPORTANT: Use pyo3-async-runtimes (NOT pyo3-asyncio which is deprecated).
  </action>
  <verify>
Run `cargo check -p marketdata-py` to verify dependencies resolve.
Run `grep -E "pyo3.*0\.27|pyo3-async-runtimes" Cargo.toml` to confirm versions.
  </verify>
  <done>
PyO3 0.27 and pyo3-async-runtimes 0.27 configured at workspace level.
Python binding compiles against new dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib.rs to Bound API and prepare async module</name>
  <files>py/src/lib.rs, py/src/errors.rs</files>
  <action>
In py/src/lib.rs:
- Update module function signature from `fn marketdata_py(m: &Bound<'_, PyModule>)` (already correct) to ensure all registrations use Bound API
- Add module-level docstring with async usage example
- Ensure all m.add_class and m.add calls use Bound patterns

In py/src/errors.rs:
- Extend exception hierarchy with create_exception! macro:
  - MarketDataError (base, already exists)
  - ApiError extends MarketDataError
  - AuthError extends MarketDataError
  - RateLimitError extends ApiError
  - ConnectionError extends MarketDataError
  - TimeoutError extends MarketDataError
  - WebSocketError extends MarketDataError
- Register all exceptions in the module
- Update to_py_err() to map core errors to specific exception types

Follow PyO3 0.27 patterns:
- Use Bound<'py, T> not &PyAny
- Use py.get_type_bound::<T>() not py.get_type::<T>()
  </action>
  <verify>
Run `cargo build -p marketdata-py 2>&1 | grep -i "deprecat"` to verify no GIL Ref deprecation warnings.
Run `cargo test -p marketdata-py` to verify error code tests pass.
  </verify>
  <done>
Module uses Bound API throughout.
Exception hierarchy includes ApiError, AuthError, RateLimitError, ConnectionError, TimeoutError, WebSocketError.
No deprecation warnings from PyO3.
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p marketdata-py` succeeds
2. `cargo build -p marketdata-py` succeeds (requires Python dev headers)
3. `grep "0.27" Cargo.toml` shows PyO3 and pyo3-async-runtimes at 0.27
4. `cargo test -p marketdata-py` passes error mapping tests
5. Build output shows no deprecation warnings for GIL Refs API
</verification>

<success_criteria>
- PyO3 0.27 compiles without deprecation warnings
- pyo3-async-runtimes 0.27 is available as workspace dependency
- Exception hierarchy ready with 6+ exception types
- Foundation ready for async API conversion in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-python-binding/02-01-SUMMARY.md`
</output>
