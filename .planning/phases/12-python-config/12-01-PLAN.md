---
phase: 12-python-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - py/src/websocket.rs
  - py/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "HealthCheckConfig can be constructed with kwargs in Python"
    - "ReconnectConfig uses core-aligned field names (max_attempts, initial_delay_ms)"
    - "Config validation fails fast with ValueError on invalid input"
    - "Config fields are readable from Python"
  artifacts:
    - path: "py/src/websocket.rs"
      provides: "HealthCheckConfig PyClass, updated ReconnectConfig"
      contains: "pub struct HealthCheckConfig"
    - path: "py/src/lib.rs"
      provides: "Module registration for HealthCheckConfig"
      contains: "m.add_class::<websocket::HealthCheckConfig>"
  key_links:
    - from: "py/src/websocket.rs HealthCheckConfig::new()"
      to: "marketdata_core::HealthCheckConfig::new()"
      via: "validation delegation"
      pattern: "marketdata_core::HealthCheckConfig::new"
    - from: "py/src/websocket.rs ReconnectConfig::new()"
      to: "marketdata_core::ReconnectionConfig::new()"
      via: "validation delegation"
      pattern: "marketdata_core::ReconnectionConfig::new"
---

<objective>
Add HealthCheckConfig PyClass and align ReconnectConfig field names with core validation

Purpose: Establish the config classes that clients will accept as parameters. Config validation
must use core's validation logic (Phase 8) to ensure consistent error messages and constraints.
Output: Two validated config PyClasses ready for use in client constructors.
</objective>

<execution_context>
@/Users/zackfan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackfan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-python-config/12-CONTEXT.md
@.planning/phases/12-python-config/12-RESEARCH.md

# Source files to modify
@py/src/websocket.rs
@py/src/lib.rs

# Core validation (Phase 8) for reference
@core/src/websocket/health_check.rs
@core/src/websocket/reconnection.rs
@core/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HealthCheckConfig PyClass</name>
  <files>py/src/websocket.rs</files>
  <action>
Add a new `HealthCheckConfig` PyClass in `py/src/websocket.rs`:

1. Define the struct with `#[pyclass]` and `#[derive(Clone)]`:
   - `enabled: bool` with `#[pyo3(get)]`
   - `interval_ms: u64` with `#[pyo3(get)]`
   - `max_missed_pongs: u64` with `#[pyo3(get)]`

2. Implement `#[pymethods]` with `#[new]` constructor:
   - Signature: `#[pyo3(signature = (*, enabled=false, interval_ms=30000, max_missed_pongs=2))]`
   - Validate by calling `marketdata_core::HealthCheckConfig::new(enabled, Duration::from_millis(interval_ms), max_missed_pongs)`
   - Map errors to `pyo3::exceptions::PyValueError::new_err(e.to_string())`
   - Store the ms values (not Duration) for FFI boundary clarity

3. Add a `to_core(&self)` method (non-PyO3) that converts to `marketdata_core::HealthCheckConfig`
   - This should use `.expect()` since validation already happened in constructor

4. Add Default impl that creates disabled config with default values

Import `std::time::Duration` at the top if not already present.

Reference the existing `ReconnectConfig` pattern in the same file for structure.
Defaults from core: DEFAULT_HEALTH_CHECK_ENABLED=false, DEFAULT_HEALTH_CHECK_INTERVAL_MS=30000, DEFAULT_HEALTH_CHECK_MAX_MISSED_PONGS=2.
  </action>
  <verify>
Run: `cd /Users/zackfan/Project/fugle/fugle-marketdata-sdk/py && cargo build 2>&1 | head -50`
Should compile without errors. Check for "Compiling marketdata-py" in output.
  </verify>
  <done>
HealthCheckConfig PyClass exists with kwargs constructor, validation via core, and readable fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ReconnectConfig field names to match core</name>
  <files>py/src/websocket.rs</files>
  <action>
Update the existing `ReconnectConfig` in `py/src/websocket.rs` to align with core's `ReconnectionConfig`:

1. Rename fields (BREAKING CHANGE - acceptable per CONTEXT.md):
   - `max_retries` -> `max_attempts`
   - `base_delay_ms` -> `initial_delay_ms`
   - Keep `enabled` and `max_delay_ms` as-is

2. Update constructor signature:
   - Change from positional to kwargs-only: `#[pyo3(signature = (*, enabled=true, max_attempts=5, initial_delay_ms=1000, max_delay_ms=60000))]`
   - Add validation by calling `marketdata_core::ReconnectionConfig::new(max_attempts, Duration::from_millis(initial_delay_ms), Duration::from_millis(max_delay_ms))`
   - Map errors to `pyo3::exceptions::PyValueError::new_err(e.to_string())`

3. Add `to_core(&self)` method that converts to `marketdata_core::ReconnectionConfig`
   - Use `.expect()` since validation happened in constructor

4. Keep static methods `default_config()` and `disabled()` for convenience but update to use new field names

5. Update Default impl to use new field names

Use core's constants: DEFAULT_MAX_ATTEMPTS=5, DEFAULT_INITIAL_DELAY_MS=1000, DEFAULT_MAX_DELAY_MS=60000.
  </action>
  <verify>
Run: `cd /Users/zackfan/Project/fugle/fugle-marketdata-sdk/py && cargo build 2>&1 | head -50`
Should compile without errors.
  </verify>
  <done>
ReconnectConfig uses core-aligned field names with kwargs constructor and validation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register HealthCheckConfig in module</name>
  <files>py/src/lib.rs</files>
  <action>
In `py/src/lib.rs`, add the HealthCheckConfig class registration:

1. Find the `marketdata_py()` function with module registration
2. Add `m.add_class::<websocket::HealthCheckConfig>()?;` after the `ReconnectConfig` registration
3. Ensure the class is importable from Python as `from marketdata_py import HealthCheckConfig`

The registration should be near line 120 where `ReconnectConfig` is registered.
  </action>
  <verify>
Run: `cd /Users/zackfan/Project/fugle/fugle-marketdata-sdk/py && cargo build && python3 -c "from marketdata_py import HealthCheckConfig, ReconnectConfig; print('OK')"`
Should print "OK" without import errors.
  </verify>
  <done>
HealthCheckConfig is importable from marketdata_py module alongside ReconnectConfig.
  </done>
</task>

</tasks>

<verification>
1. Both config classes compile: `cargo build` succeeds
2. Both classes are importable: `python3 -c "from marketdata_py import HealthCheckConfig, ReconnectConfig"`
3. Config construction works:
   ```python
   from marketdata_py import HealthCheckConfig, ReconnectConfig
   hc = HealthCheckConfig(enabled=True, interval_ms=15000)
   rc = ReconnectConfig(max_attempts=10, initial_delay_ms=2000)
   print(f"HC enabled={hc.enabled}, interval={hc.interval_ms}")
   print(f"RC attempts={rc.max_attempts}, delay={rc.initial_delay_ms}")
   ```
4. Validation errors work:
   ```python
   try:
       HealthCheckConfig(interval_ms=1000)  # Below 5000ms minimum
   except ValueError as e:
       print(f"Expected error: {e}")
   ```
</verification>

<success_criteria>
- HealthCheckConfig PyClass with enabled, interval_ms, max_missed_pongs fields
- ReconnectConfig with renamed fields: max_attempts, initial_delay_ms
- Both use kwargs-only constructors with sensible defaults
- Both validate via core's validation logic, failing with ValueError
- Both are registered and importable from marketdata_py
</success_criteria>

<output>
After completion, create `.planning/phases/12-python-config/12-01-SUMMARY.md`
</output>
