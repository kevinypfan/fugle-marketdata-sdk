---
phase: 01-build-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - core/Cargo.toml
  - py/Cargo.toml
  - js/Cargo.toml
  - uniffi/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "All crates compile successfully from workspace root with cargo build"
    - "Shared dependencies (tokio, serde, serde_json) appear once in workspace root"
    - "Version 0.2.0 is defined once in root and inherited by all members"
    - "resolver = 2 prevents feature unification pitfalls"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root configuration"
      contains: "[workspace]"
    - path: "Cargo.lock"
      provides: "Shared lockfile for all members"
    - path: "core/Cargo.toml"
      provides: "Core library with workspace inheritance"
      contains: "version.workspace = true"
    - path: "py/Cargo.toml"
      provides: "Python binding with workspace inheritance"
      contains: "version.workspace = true"
    - path: "js/Cargo.toml"
      provides: "Node.js binding with workspace inheritance"
      contains: "version.workspace = true"
    - path: "uniffi/Cargo.toml"
      provides: "C# binding with workspace inheritance"
      contains: "version.workspace = true"
  key_links:
    - from: "*/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace inheritance"
      pattern: "\\.workspace\\s*=\\s*true"
    - from: "py/Cargo.toml"
      to: "core/Cargo.toml"
      via: "path dependency"
      pattern: "marketdata-core.workspace = true"
---

<objective>
Create a Cargo workspace root and migrate all existing crates to use workspace dependency inheritance.

Purpose: Establish the foundation for unified dependency management, version synchronization, and build optimization across all language bindings. This enables shared caching, consistent versioning, and unlocks parallel Phase 2/3 work.

Output: Working Cargo workspace with all four members (core, py, js, uniffi) using shared dependencies and synchronized versioning at 0.2.0.
</objective>

<execution_context>
@/Users/zackfan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackfan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-infrastructure/01-RESEARCH.md
@.planning/phases/01-build-infrastructure/01-CONTEXT.md

Current Cargo.toml files to migrate:
@core/Cargo.toml
@py/Cargo.toml
@js/Cargo.toml
@uniffi/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace root Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>
Create a new root Cargo.toml with:

1. **[workspace] section:**
   - members = ["core", "py", "js", "uniffi"]
   - resolver = "2" (CRITICAL: prevents feature unification pitfalls)

2. **[workspace.package] section** (shared metadata):
   - version = "0.2.0" (bump from 0.1.0 to reflect workspace migration)
   - edition = "2021"
   - license = "MIT"
   - repository = "https://github.com/fugle/fugle-marketdata-sdk"
   - homepage = "https://developer.fugle.tw"

3. **[workspace.dependencies] section** with ALL shared deps:
   - tokio = { version = "1.49", features = ["rt", "rt-multi-thread", "sync", "time", "macros"] }
   - serde = { version = "1.0", features = ["derive"] }
   - serde_json = "1.0"
   - thiserror = "2.0"
   - anyhow = "1.0"
   - futures-util = "0.3"
   - url = "2.5"

   Python binding deps:
   - pyo3 = { version = "0.22", features = ["extension-module"] }

   Node.js binding deps:
   - napi = { version = "2.16", features = ["napi8", "async", "serde-json", "tokio_rt"] }
   - napi-derive = "2.16"
   - napi-build = "2.1.3"

   C# binding deps (UniFFI for now, csbindgen in Phase 4):
   - uniffi = { version = "0.28", features = ["cli"] }

   Path dependency for core:
   - marketdata-core = { path = "core" }

Do NOT include ureq, tokio-tungstenite, exponential-backoff, indexmap in workspace.dependencies - these are core-only deps and should stay in core/Cargo.toml.
  </action>
  <verify>
Run: `cargo metadata --format-version 1 | jq '.workspace_root'`
Should return the project root path, confirming workspace is recognized.
  </verify>
  <done>Root Cargo.toml exists with [workspace], [workspace.package], and [workspace.dependencies] sections. cargo metadata recognizes workspace structure.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate member Cargo.toml files to workspace inheritance</name>
  <files>core/Cargo.toml, py/Cargo.toml, js/Cargo.toml, uniffi/Cargo.toml</files>
  <action>
Update each member Cargo.toml to use workspace inheritance:

**core/Cargo.toml:**
- Add: version.workspace = true, edition.workspace = true, license.workspace = true
- Keep package name as "marketdata-core"
- Remove explicit version, edition
- Replace shared deps with .workspace = true syntax:
  - tokio.workspace = true (but KEEP the features override for this crate)
  - serde.workspace = true
  - serde_json.workspace = true
  - thiserror.workspace = true
  - anyhow.workspace = true
  - futures-util.workspace = true
  - url.workspace = true
- Keep core-only deps as-is (ureq, tokio-tungstenite, exponential-backoff, indexmap)
- Keep [features], [dev-dependencies], [[bench]] sections unchanged

**py/Cargo.toml:**
- Add: version.workspace = true, edition.workspace = true, license.workspace = true
- Keep package name as "marketdata-py", description
- Remove explicit version, edition
- Replace deps with workspace inheritance:
  - marketdata-core.workspace = true
  - pyo3.workspace = true
  - serde_json.workspace = true
  - tokio.workspace = true (override features: ["rt-multi-thread", "sync"])
- Keep [lib] section unchanged (name = "marketdata_py", crate-type = ["cdylib"])

**js/Cargo.toml:**
- Add: version.workspace = true, edition.workspace = true, license.workspace = true
- Keep package name as "marketdata-js", description
- Remove explicit version, edition
- Replace deps with workspace inheritance:
  - marketdata-core.workspace = true
  - napi.workspace = true
  - napi-derive.workspace = true
  - serde.workspace = true
  - serde_json.workspace = true
  - tokio.workspace = true (override features: ["rt", "time", "sync"])
- Replace build-deps: napi-build.workspace = true
- Keep [lib] section unchanged

**uniffi/Cargo.toml:**
- Already has workspace syntax, just verify it's correct
- Ensure marketdata-core uses workspace inheritance: marketdata-core.workspace = true
- Keep [lib], [build-dependencies], [dev-dependencies] as-is
  </action>
  <verify>
Run from project root:
```bash
cargo check --workspace
```
Should compile all four crates without errors.

Then verify workspace inheritance:
```bash
cargo metadata --format-version 1 | jq '.packages[] | select(.name | startswith("marketdata")) | {name: .name, version: .version}'
```
All four packages should show version "0.2.0".
  </verify>
  <done>All member Cargo.toml files use workspace inheritance. `cargo check --workspace` succeeds. All members report version 0.2.0.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Workspace structure valid:**
   ```bash
   cargo metadata --format-version 1 | jq '.workspace_members | length'
   ```
   Should return: 4

2. **Version synchronization:**
   ```bash
   cargo metadata --format-version 1 | jq -r '.packages[] | select(.name | startswith("marketdata")) | "\(.name): \(.version)"'
   ```
   Should show all at 0.2.0

3. **Full workspace build:**
   ```bash
   cargo build --workspace
   ```
   Should succeed with no errors

4. **Shared Cargo.lock exists:**
   ```bash
   ls -la Cargo.lock
   ```
   Should exist at workspace root (not in member directories)
</verification>

<success_criteria>
- Root Cargo.toml exists with workspace configuration
- All 4 members (core, py, js, uniffi) use workspace inheritance
- `cargo build --workspace` succeeds
- All members report version 0.2.0
- Single Cargo.lock at workspace root
- No duplicate dependency versions across members
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-infrastructure/01-01-SUMMARY.md`
</output>
