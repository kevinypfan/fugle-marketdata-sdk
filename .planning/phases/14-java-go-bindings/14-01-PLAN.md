---
phase: 14-java-go-bindings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bindings/java/src/main/java/tw/com/fugle/marketdata/FugleRestClient.java
  - bindings/java/src/main/java/tw/com/fugle/marketdata/FugleWebSocketClient.java
  - bindings/java/src/main/java/tw/com/fugle/marketdata/ReconnectOptions.java
  - bindings/java/src/main/java/tw/com/fugle/marketdata/HealthCheckOptions.java
  - bindings/java/src/test/java/tw/com/fugle/marketdata/ConfigOptionsTest.java
autonomous: true

must_haves:
  truths:
    - "Java RestClient builder accepts apiKey, bearerToken, or sdkToken with exactly-one-auth validation"
    - "Java RestClient builder accepts optional baseUrl"
    - "Java WebSocketClient builder accepts bearerToken and sdkToken (not just apiKey)"
    - "Java WebSocketClient builder accepts optional ReconnectOptions and HealthCheckOptions"
    - "Invalid config values throw FugleException with descriptive messages including field name and constraint"
    - "Default config values match core constants (maxAttempts=5, initialDelayMs=1000, maxDelayMs=60000, etc.)"
  artifacts:
    - path: "bindings/java/src/main/java/tw/com/fugle/marketdata/ReconnectOptions.java"
      provides: "Reconnect config with builder pattern"
      contains: "maxAttempts"
    - path: "bindings/java/src/main/java/tw/com/fugle/marketdata/HealthCheckOptions.java"
      provides: "Health check config with builder pattern"
      contains: "intervalMs"
    - path: "bindings/java/src/main/java/tw/com/fugle/marketdata/FugleRestClient.java"
      provides: "Updated builder with exactly-one-auth validation and baseUrl"
      contains: "bearerToken"
    - path: "bindings/java/src/main/java/tw/com/fugle/marketdata/FugleWebSocketClient.java"
      provides: "Updated builder with all auth methods and config options"
      contains: "reconnectOptions"
    - path: "bindings/java/src/test/java/tw/com/fugle/marketdata/ConfigOptionsTest.java"
      provides: "Unit tests for config validation and builder patterns"
      contains: "testExactlyOneAuth"
  key_links:
    - from: "FugleRestClient.Builder.build()"
      to: "MarketdataUniffi.newRestClientWithApiKey/BearerToken/SdkToken"
      via: "exactly-one-auth validation then dispatch"
      pattern: "authCount.*==.*0|authCount.*>.*1"
    - from: "FugleWebSocketClient.Builder.build()"
      to: "ReconnectOptions/HealthCheckOptions"
      via: "builder methods accepting config objects"
      pattern: "reconnect.*ReconnectOptions|healthCheck.*HealthCheckOptions"
---

<objective>
Add config exposure to Java bindings via builder pattern

Purpose: Java users can construct RestClient and WebSocketClient with idiomatic builder API supporting all auth methods, baseUrl override, and WebSocket configuration (reconnect, health check).

Output: Updated Java wrapper classes with builder pattern, config option classes, and unit tests.
</objective>

<execution_context>
@/Users/zackfan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackfan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-java-go-bindings/14-RESEARCH.md

# Existing Java binding files to modify
@bindings/java/src/main/java/tw/com/fugle/marketdata/FugleRestClient.java
@bindings/java/src/main/java/tw/com/fugle/marketdata/FugleWebSocketClient.java
@bindings/java/src/main/java/tw/com/fugle/marketdata/FugleException.java

# Core constants reference (line 50-57 has DEFAULT_* constants)
@core/src/lib.rs

# UniFFI lib.rs for available constructor functions
@uniffi/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReconnectOptions and HealthCheckOptions config classes</name>
  <files>
    bindings/java/src/main/java/tw/com/fugle/marketdata/ReconnectOptions.java
    bindings/java/src/main/java/tw/com/fugle/marketdata/HealthCheckOptions.java
  </files>
  <action>
Create two Java config classes with builder patterns:

**ReconnectOptions.java:**
- Immutable class with fields: `Integer maxAttempts`, `Long initialDelayMs`, `Long maxDelayMs`
- All fields nullable (Optional via null) — null means use default
- Static inner `Builder` class with fluent methods returning `this`
- `builder()` static factory method
- `build()` method that creates immutable instance
- Getter methods for each field: `getMaxAttempts()`, `getInitialDelayMs()`, `getMaxDelayMs()`
- Document default values in Javadoc: maxAttempts=5, initialDelayMs=1000, maxDelayMs=60000

**HealthCheckOptions.java:**
- Immutable class with fields: `Boolean enabled`, `Long intervalMs`, `Long maxMissedPongs`
- All fields nullable — null means use default
- Static inner `Builder` class with fluent methods
- `builder()` static factory method
- Getter methods for each field
- Document default values in Javadoc: enabled=false, intervalMs=30000, maxMissedPongs=2

Both classes should:
- Be `public final` (immutable after construction)
- Store fields via private constructor called by Builder.build()
- NOT validate at build time — validation happens in the client builder where core constants are available
- Follow same package: `tw.com.fugle.marketdata`
- Follow existing code style (see FugleException.java for reference)
  </action>
  <verify>
`cd bindings/java && ./gradlew compileJava` compiles without errors. Both config classes exist with builder pattern.
  </verify>
  <done>
ReconnectOptions and HealthCheckOptions classes exist with builder patterns, immutable fields, and Javadoc documenting defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update client builders with auth validation, config support, and tests</name>
  <files>
    bindings/java/src/main/java/tw/com/fugle/marketdata/FugleRestClient.java
    bindings/java/src/main/java/tw/com/fugle/marketdata/FugleWebSocketClient.java
    bindings/java/src/test/java/tw/com/fugle/marketdata/ConfigOptionsTest.java
  </files>
  <action>
**Update FugleRestClient.Builder:**
1. Add `bearerToken(String)` and `sdkToken(String)` builder methods (bearerToken already exists from Phase 4.2, verify and keep)
2. Add `baseUrl(String)` builder method with field storage
3. Update `build()` with exactly-one-auth validation:
   - Count non-null auth fields (apiKey, bearerToken, sdkToken)
   - If 0: throw FugleException("Provide exactly one of: apiKey, bearerToken, sdkToken")
   - If >1: throw FugleException("Provide exactly one of: apiKey, bearerToken, sdkToken")
   - Match error message format from Python/Node.js phases
4. After creating RestClient via UniFFI, apply baseUrl if provided:
   - NOTE: Core RestClient has `base_url()` builder method (consumes self). Since UniFFI RestClient wraps an Arc, baseUrl cannot be set post-construction via UniFFI. Store baseUrl and add a TODO comment noting this needs a UniFFI-exposed setter. This matches the same approach used in Python (Phase 12-02) and Node.js (Phase 13-02).

**Update FugleWebSocketClient.Builder:**
1. Add `bearerToken(String)` and `sdkToken(String)` builder methods
2. Add `baseUrl(String)` builder method
3. Add `reconnect(ReconnectOptions)` builder method storing options
4. Add `healthCheck(HealthCheckOptions)` builder method storing options
5. Update `build()` with exactly-one-auth validation (same as REST above)
   - Currently only validates apiKey; extend to support all three auth methods
   - Select the correct UniFFI constructor based on which auth method is set
   - NOTE: Current UniFFI WebSocketClient constructors only accept api_key. For bearerToken/sdkToken, store the value and add TODO (same pattern as Python/Node.js phases where WebSocket currently only uses apiKey internally)
6. Store reconnectOptions and healthCheckOptions for future use (same pattern as Python Phase 12-02 — configs stored but not yet wired to core WebSocket, with TODO comment)

**Create ConfigOptionsTest.java:**
Unit tests verifying:
1. `testReconnectOptionsBuilder()` — build with custom values, verify getters
2. `testReconnectOptionsDefaults()` — build with no values, verify nulls (defaults applied by client)
3. `testHealthCheckOptionsBuilder()` — build with custom values, verify getters
4. `testHealthCheckOptionsDefaults()` — build with no values, verify nulls
5. `testRestClientExactlyOneAuth_apiKey()` — apiKey alone works (no exception)
6. `testRestClientExactlyOneAuth_bearerToken()` — bearerToken alone works
7. `testRestClientExactlyOneAuth_sdkToken()` — sdkToken alone works
8. `testRestClientNoAuth()` — no auth throws FugleException with correct message
9. `testRestClientMultipleAuth()` — two auth methods throws FugleException
10. `testWebSocketExactlyOneAuth_apiKey()` — apiKey alone works
11. `testWebSocketNoAuth()` — no auth throws FugleException
12. `testWebSocketMultipleAuth()` — two auth methods throws FugleException
13. `testWebSocketWithReconnectOptions()` — builder accepts ReconnectOptions without error
14. `testWebSocketWithHealthCheckOptions()` — builder accepts HealthCheckOptions without error

For auth validation tests that call build(), catch the expected exception type (FugleException) and verify the message contains "Provide exactly one of". For tests that successfully build, they will throw on actual UniFFI call (no real API key) — use try/catch and verify the exception is NOT from auth validation (i.e., it's a MarketDataException from the actual connection attempt, not a FugleException from validation).

Use JUnit 5 annotations (@Test, assertions from org.junit.jupiter.api.Assertions). Check existing test files in `bindings/java/src/test/java/tw/com/fugle/marketdata/` for the test framework and import patterns used.
  </action>
  <verify>
`cd bindings/java && ./gradlew test` — ConfigOptionsTest tests pass. All auth validation tests verify correct exception messages.
  </verify>
  <done>
RestClient builder validates exactly-one-auth with descriptive error messages. WebSocketClient builder accepts all auth methods plus ReconnectOptions and HealthCheckOptions. 14 unit tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd bindings/java && ./gradlew compileJava` — compiles without errors
- `cd bindings/java && ./gradlew test` — all tests pass including new ConfigOptionsTest
- FugleRestClient.builder().apiKey("x").build() still works (backwards compatible)
- FugleRestClient.builder().build() throws FugleException with "Provide exactly one of: apiKey, bearerToken, sdkToken"
- FugleWebSocketClient.builder().apiKey("x").reconnect(ReconnectOptions.builder().maxAttempts(10).build()).stock().build() compiles
</verification>

<success_criteria>
- Java RestClient builder enforces exactly-one-auth with descriptive error
- Java WebSocketClient builder accepts ReconnectOptions and HealthCheckOptions
- All 14 unit tests pass
- Existing RestClient.builder().apiKey("key").build() pattern still works unchanged
- Config option classes have proper Javadoc with default values documented
</success_criteria>

<output>
After completion, create `.planning/phases/14-java-go-bindings/14-01-SUMMARY.md`
</output>
