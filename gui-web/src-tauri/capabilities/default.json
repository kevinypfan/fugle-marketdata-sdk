{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — core APIs plus persistence and logging",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "log:default"
  ]
}
